pub mod nn {
    use crate::nn::layer::layer::Layer;

    pub struct NeuralNetwork {
        pub layers: Vec<Layer>,
        pub num_layers: usize
    }

    impl NeuralNetwork {
        pub fn new
    }
}